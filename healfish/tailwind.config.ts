// Tailwind v4 is configured via CSS (@theme in globals.css), not this file.
// This file is kept for tooling compatibility.
import type { Config } from "tailwindcss";
const config: Config = { content: [] };
export default config;
